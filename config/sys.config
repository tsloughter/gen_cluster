[
 {gen_cluster, [{discovery, {gc_discover_epmd_hosts, #{}}}]},

 {kernel,
  [{logger_level, debug},
   {logger,
    [{handler, default, logger_std_h,
      #{ formatter => {logger_formatter, #{}}}}]}]}
].
